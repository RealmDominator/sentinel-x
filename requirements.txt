# SENTINEL-X runtime dependencies
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
pydantic>=2.7

androguard>=4.1.0        # APK parsing (manifest, permissions, certs, DEX)
xgboost>=2.1             # malware classifier
scikit-learn>=1.5        # isotonic calibration, metrics, splits
shap>=0.46               # verdict explainability
numpy>=1.26
pandas>=2.2
joblib>=1.4

networkx>=3.2            # certificate relationship graph
fpdf2>=2.7               # PDF intelligence report

anthropic>=1.2           # optional: live GenAI narrative (template fallback otherwise)
pyzipper>=0.3.6          # optional: decrypt MalwareBazaar AES zips in memory (real-data scripts)
pytest>=8.0              # tests

# --- Optional: local dynamic analysis (SENTINELX_DYNAMIC_BACKEND=emulator).
# Not needed for static analysis, and the sinkhole is stdlib-only by design.
# adb + emulator come from the Android SDK; scripts/setup_dynamic.py checks them.
# frida>=16.0            # runtime instrumentation
# frida-tools>=12.0
